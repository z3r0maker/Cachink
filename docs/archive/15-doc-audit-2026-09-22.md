> **ARCHIVED 2026-09-22.** A snapshot of the same day's doc audit; it says so itself. What it changed is in git; what it left open is on the generated board `docs/plan/PENDIENTES.md`.
>
> Kept for its rationale and history. Do not plan from it.

# Doc audit — 2026-09-22

Every unticked item in `docs/` was checked against the code (six parallel passes, each citing
paths). This file records what changed and what is genuinely left, so the next session does not
re-derive it. It is a snapshot, not a board: the live boards stay
`docs/plan/13-web-portal-handoff.md` (cross-track), `docs/plan/10-operador.md` (register) and each
track file.

## What this audit changed

- **Archived** (moved to `docs/archive/`, each with a "superseded by" header):
  `billing-and-licensing-plan.md`, `billing-stripe-only-plan.md`, `CLOUD-MODE-PLAN.md`,
  `e2e-HANDOFF.md`, `e2e-mvp-code-changes-2026-07-09.md`,
  `e2e-productos-row-accessibility-scope.md`, `plan/10-operador-handoff.md`. The two notes that
  outlived the E2E docs moved into `apps/mobile/maestro/E2E-SESSION-NOTES.md` §6.
- **Ticked with evidence** in `04-portal.md`: P-02, P-05, P-08, P-09, P-10, P-11, P-12, P-13,
  P-14, P-17, P-24, P-25, P-26, P-27, P-31, P-33, P-34 — all shipped with Playwright coverage.
  P-06, P-23, P-28 and P-30 stay `[~]` with a line naming what is left.
- **Corrected statuses:** `05-app.md` carried six duplicate `- [ ]` lines above the real `- [x]`
  (deleted); `04-portal.md` had two stray status lines belonging to P-13/P-14 (annotated);
  `03-backend.md` B-10 was `[ ]` though Stripe checkout, webhook, mapping and idempotency ledger
  are built (now `[~]` with the two real gaps); `08-post-launch.md` Z-06 shipped with P-34.
- **Refreshed numbers** in `12-glm-handoff.md` and `13-web-portal-handoff.md`: data-pg next free
  **0033**, SQLite **0012**, backoffice **0015**, ADR **ADR-092**. `12-glm-handoff.md` §2.2's
  "blocked on the app branch" premise died when that branch merged (`52824588`, 2026-09-19).
- **Flagged:** `docs/testing/e2e-test-catalog.md` marks flows ✅ that were deleted or parked.

## What is genuinely pending

**Phone half (the keystone).** C-15 and C-20 have their Postgres halves; SQLite stops at
migration 0011, so branding and opening balances never reach a device
(`packages/data/src/repositories/drizzle/businesses-repository.ts` drops seven columns). Behind it:
N-19, N-17, then N-22 sync banners, N-25 QR pairing (needs C-14) and N-24's restyle. A-15/A-16
reduce to the Maestro harness still entering through archived first-run subflows.

**Portal.** P-29 (catálogo desde una foto) unbuilt; P-28's report body; P-30's cron/route/batch;
P-06's QR panel (blocked on C-14); P-23's `design:compare` gate.

**Backend.** Three email templates (`welcome`, `payment-failed`, `factura-issued`); B-16's
subscriptions-by-plan query; `BUSINESS_SUSPENDED` never thrown; B-04's App Review tenant.

**Pre-launch gates.** From `docs/audits/`: ~~no `business_id`-leading indexes~~ (fixed 2026-09-22,
data-pg 0033 + two guards), no
foreign keys at all, the legacy `supabase/migrations` still present, no CSP/HSTS headers
(`apps/web/next.config.mjs` has no `headers()`), no dependency/secret/SAST scan in CI, no OTA code
signing, device tokens without `kid`/`jti`. Five of six high-severity security findings are fixed.

**Launch.** X-01…X-10 almost entirely open; `docs/launch-checklist.md` is pre-rebrand (it still
buys `cachink.mx` and deploys `docs/landing/index.html`) — X-10 owns rewriting it. Most of the rest
is owner-only: domain, Apple/Google accounts, Sentry, store submission, backups/PITR.

**Post-launch, correctly parked.** Payments (N-40…N-45, N-53) have no code and are trigger-gated.

## Tooling finding

`pnpm lint:design` is **not wired into CI**. Its baseline (`.design-lint-baseline.json`, recorded
2026-09-08) says zero; the tree now has 60 findings — 34 font-size literals (login animations,
WhatsApp dialogs, comprobantes, saldos), 13 duplicated inline hexes, 6 off-scale radii. Either
ratchet it in CI or re-baseline deliberately.

## Branch and worktree state (same date)

- **`track/app` is the only branch with unmerged commits** (4, from 2026-09-16/17): the A-16
  Maestro work plus a WIP commit that is mostly `brag-output/` video files. The phone track landed
  on `main` later by another route, and `main` now has more flows (136 vs 113) — but three of the
  branch's test files are absent from `main` (`cerrar-caja-modal`, `use-keyboard-height`,
  `editar-egreso-modal`). Cherry-pick those, then delete the branch; merging it wholesale would
  drag back 332 commits of drift.
- Work in progress elsewhere at audit time: the backoffice auth/MFA changes in the main checkout,
  `track-o/o38` (closed by `d0bca546`), one stash on `main`.
