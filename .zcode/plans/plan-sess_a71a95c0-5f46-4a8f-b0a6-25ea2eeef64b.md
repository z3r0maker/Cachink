# Track N pending-work program — agreed plan (interview 2026-09-18)

Execution order = handoff §3: **N-31 → N-16→N-18 → C-15→N-19→N-20 → C-20→N-17 → backoffice follow-ups**, N-33 gaps slotted between epics. C-12 held until O-20 (owner unanswered; safe default). N-34 out (counsel). All work in a git worktree on `track-n/*` branches; push protocol per handoff §0 (fetch+merge, typecheck/lint/test, push from clean tree; never force-push). I report each data-pg migration number (0020+) and backoffice number (0011+) before pushing. ADR-086 records the design-governance exception (utility screens code-first under DESIGN_CONTRACT.md + design-lint; receipts design-first).

## Epic 1 — N-31 Landing rebrand (branch `track-n/n31-landing`)

Facts from exploration: pricing is currently Gratis$0/Pro$149/Contador$299 with **inert** CTAs; `test` = build + prerender smoke tests that assert today's hero copy; sitemap.xml hand-written; og-image generated by `scripts/generate-og.mjs` (sharp); no manifest exists.

1. **Brand sweep**: every "Cachink" → "Xangarro!" in `landing/copy.jsx` (TONE_COPY, FAQ), `Sections.jsx`, `AnimatedHero.jsx`, `ContactoFooter.jsx`, `ParaQuienEs.jsx`, `Viewport.jsx`, `PhoneScreens.jsx`, `src/global.css`, `Recursos.jsx`, 4 articles, `index.html` (title/OG/twitter/application-name), `structured-data.js`, `scripts/prerender.mjs` (titles/descriptions + smoke assertions updated in lockstep), `scripts/generate-og.mjs` wordmark, `public/robots.txt|llms.txt|llms-full.txt|sitemap.xml`. Yellow #FFD60A/black kept.
2. **Canonical domain**: `VITE_SITE_URL` default + `.env.example` + `vite.config.js` + `prerender.mjs` L162 domain-replace + `structured-data.js` → `https://xangarro.mx`.
3. **Precios rebuild** (`landing/sections/Precios.jsx`): xangarrito $0 · xangarro $199 · xangarrote $399 MXN/mes ("Recomendado" on xangarro), monthly/annual toggle (annual = $1,990/$3,990, "2 meses gratis"), every price "**+ IVA**" with total in a footnote, limits 300/50 · 10k/1k · 30k/5k tx/productos, operadores 1/2/5, export on every tier, NIF statements paid-only, payment line "Tarjeta de crédito o débito · Transferencia SPEI en plan anual" (no OXXO), prices from one constants module.
4. **CTAs (L-03)**: hero + tier CTAs → `https://app.xangarro.mx/signup?plan=xangarrito|xangarro|xangarrote`; replace the "Lista de espera" waitlist CTA with "Crear cuenta"; UTM passthrough.
5. **Assets**: favicon-32/apple-touch-icon/og-image regenerated as yellow-black text-wordmark placeholders via the existing sharp script; add a minimal `site.webmanifest`; hero tagline keeps "Finanzas para tu negocio…", plus the offline line "Tu negocio sigue aunque se vaya el internet" in copy/FAQ.
6. Mirror `structured-data.js` offers + `llms.txt` Planes section to the new tiers; delete stale `docs/landing/index.html` (note in Done line).
7. Verify: `grep -rni cachink apps/landing docs/landing` → 0 (blog copy included; no historical-changelog exception applies); `pnpm --filter @xangarro/landing build` green (prerender smoke tests updated); manual responsive check at 360px.

## Epic 2 — N-16 import engine + Clientes (branch `track-n/n16-import`)

Generalize P-07 into a template registry **keeping products behaviour and the action signatures identical**: `readSheet` stays; per-template (columns, parser, existing-matcher, applier) registered in `apps/web/src/server/import/`; pure planning/validation moves to testable units; engine stays in apps/web (ExcelJS + tx are server concerns); apply-through-use-cases pattern kept (new `CrearClienteUseCase`/`EditarClienteUseCase` in `packages/application`, pg clients repo). **Clientes matching: teléfono first, strictly-normalized nombre fallback; matches = "actualizar" (overwrite teléfono/RFC); no-name rows, in-file dups, invalid RFC (`packages/domain/src/fiscal/rfc.ts`) = dry-run error rows.** UI: unified **`/importar`** screen with template cards (Productos, Clientes; Saldos + "Hazlo por mí" card arrive later); `/productos/importar` 301s; nav "Importar". Tests per N-16 acceptance: malformed row, >5 000 rows, duplicate, happy path — per template; plus Playwright spec on the web e2e suite. CSV support added (spec: .xlsx/.csv) alongside ExcelJS.

## Epic 3 — C-15 + N-19 branding (+ N-20 when designs land) (branch `track-n/c15-n19-branding`)

- **C-15** contract: `brand_color`, `receipt_template ∈ clasico|moderno|ticket|minimal`, `receipt_leyenda`, `address_print`, `whatsapp`, `social_links` (JSON) on wire + pg (SQLite half waits for app branch; drift test scope respected); data-pg **migration 0020** also creates public bucket `negocio-logos` (storage.buckets row + write RLS per business_id folder).
- **N-19**: upload action (owner/admin) PNG/JPG/SVG ≤ 2 MB; SVG sanitised server-side (strip scripts/event handlers/foreignObject/external refs) before store; `businesses.logo_url` = stable public URL; brand colour auto-extracted via a pure function in packages/domain (raster histogram, dominant SVG fill), always editable; Comprobantes section in `negocio/edicion` (same tabbed-form pattern); logo rendered in the portal sidebar brand block when set; malicious-SVG test + old→new migration test on a throwaway Docker PG. Monthly-PDF logo wiring where the informe generator renders it.
- **N-20** (starts only when owner mirrors the four receipt designs): one renderer in `packages/domain/src/comprobante/` (Clásico/Moderno/Ticket/Minimal), parameterized by C-15 fields; contrast-aware text colour; PNG + PDF outputs; snapshot tests per template; portal live preview in Comprobantes.

## Epic 4 — C-20 + N-17 opening balances (branch `track-n/c20-n17-apertura`)

- **C-20** contract + pg half: `opening_balances` (fecha_apertura, caja_centavos, bancos_centavos, locked_at, updated_at) + `opening_balance_clients` (cliente_id, saldo_centavos); data-pg **migration 0021** (or next free); old→new test; SQLite half deferred (app branch).
- **N-17**: saldos iniciales as a third import template (caja, bancos, CxC per cliente matched by the N-16 cliente rule) + review screen (Configuración → Saldos iniciales) editable until locked; **"Bloquear saldos iniciales" typed-confirm action sets `locked_at`** (no period-close machinery invented). **Inventario inicial**: dedicated one-time screen — editable grid of active products (cantidad + costo, live valuation total) with Excel/CSV pre-fill through the registry (dry-run row errors, then fills the grid); save writes portal movements `origen=apertura` in one transaction (excluded from usage; feeds opening inventory valuation). Extend the domain balance calculators to take opening balances (today `calculateBalanceGeneral` gets empty inputs — estados.ts:64-71) and wire `loadEstadosModel` to use them; statements tests per N-17 acceptance. `origen` column: only what C-20/N-17 need without C-12 (apertura tagging lands with C-12 after O-20; if C-12 stays held, apertura movements are identified via the portal-device rule already in `usage_counts()` — verify and document).

## Epic 5 — Backoffice follow-ups (branch `track-n/backoffice-followups`)

- `apps/backoffice/e2e/` Playwright suite + config that refuses to run without a local DB (web's pattern): 403 non-allowlisted, forced TOTP enrolment, sign-out (fresh context), lockout (own `x-forwarded-for`), audit-row-per-mutation; CI job `backoffice-e2e` (postgres:17 service) in `ci.yml` — **owner adds it to branch protection**.
- Inbox sources: portal `/ayuda` page (sidebar-footer link) posting to the ingest endpoint; extend support-item kind enum with `ayuda` (backoffice **migration 0011**); rewrite `supabase/functions/bug-report` → POST `/api/internal/support-items` with shared secret.
- `staff_sessions` pruning + `assisted_imports` purge (epic 6) as steps in the existing digest cron; last-owner-login column on tenants; consolidate the 3 "last seen" implementations into one data-pg query.

## Epic 6 — N-18 "Hazlo por mí" (branch `track-n/n18-hazlo-por-mi`; needs N-16 merged)

Card on `/importar` (paid tiers; xangarrito sees "disponible en planes de pago") → form (sistema actual, qué datos, ≤5 files ≤20 MB) → private bucket `migraciones` → inbox `kind=migracion`. `assisted_imports` state machine (revision → esperando_aprobacion → aplicada/rechazada/expirada); staff dry-run from backoffice (audited) → tenant approves on `/importar` (same dry-run table renderer); commit re-reads the stored file server-side and checks status at the data layer. Purge (30 d post-resolution, delete + log) runs in the digest cron.

## N-33 CFDI gaps (branch `track-n/n33-gaps`, slotted early — small)

Refunds listed in Facturas as "Reembolso" + Stripe `charge.refunded`/`refund.created` webhook → `factura` inbox item for manual-phase cancellation (no automated cancellation/egreso until O-14); `claimed` payments display as pendiente (not error); resolving the `cfdi-global:<period>` item marks its payments; `db-local.sh` provisions `xangarro_admin` so "marcar UUID" works locally.

## Constants

- Out of scope: C-12 app half + N-04/N-02/N-03 finishes (held for O-20 unless owner says otherwise), N-34 (counsel), N-11/P-10/N-23 (portal session), N-26/27/28/30 (staging), N-07 (Track B timing table — raise with portal session).
- Plan hygiene: tick tasks + append Done lines in `docs/plan/*.md` (never renumber); ADR-086 in ARCHITECTURE.md; CLAUDE.md untouched.
- Owner dependencies: receipt designs mirrored before N-20 (epic 3 tail); branch-protection flip after `backoffice-e2e` exists; Facturapi test keys for sandbox stamps; O-20 timeline decision for C-12.

## Verification

Per epic: `pnpm --filter <pkg> test` + `pnpm typecheck && pnpm lint` in the worktree; migration tests on throwaway Docker PG (`db-local.sh`); web/backoffice Playwright where screens changed; push per the clean-tree protocol, reporting migration numbers taken. Known flaky tests re-run once, never disabled.
