{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "@cachink/domain#build",
        "@cachink/data#build",
        "@cachink/application#build"
      ]
    }
  }
}
