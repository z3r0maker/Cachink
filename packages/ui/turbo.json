{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "@xangarro/domain#build",
        "@xangarro/data#build",
        "@xangarro/application#build"
      ]
    }
  }
}
