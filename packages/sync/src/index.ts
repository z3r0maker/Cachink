/**
 * `@xangarro/sync` — the capture app's cloud sync (ADR-053 §6).
 * API client, reference-data applier; push/pull/orchestrator land in A-06.
 */

export * from './api-client.js';
export * from './reference-applier.js';
