{
  "name": "@xangarro/data-pg",
  "version": "0.0.0",
  "private": true,
  "description": "Postgres (Drizzle pg-core) schema for the cloud database, plus the drift test that keeps it aligned with the device's SQLite schema.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --config ../../eslint.config.js",
    "lint:fix": "eslint . --config ../../eslint.config.js --fix",
    "clean": "rm -rf dist .turbo",
    "db:generate": "drizzle-kit generate",
    "db:up": "./scripts/db-local.sh up",
    "db:down": "./scripts/db-local.sh down",
    "test:db": "REQUIRE_DB=1 DATABASE_URL=$(./scripts/db-local.sh url) DATABASE_SUPER_URL=$(./scripts/db-local.sh super-url) vitest run",
    "db:seed": "DATABASE_URL=$(./scripts/db-local.sh url) tsx ./scripts/seed.ts && BILLING_DATABASE_URL=$(./scripts/db-local.sh billing-url) tsx ./scripts/seed-billing.ts",
    "db:apply": "./scripts/db-local.sh apply",
    "db:reset": "./scripts/db-local.sh down && ./scripts/db-local.sh up && pnpm db:seed"
  },
  "dependencies": {
    "@xangarro/contracts": "workspace:*",
    "@xangarro/domain": "workspace:*",
    "bcryptjs": "3.0.3",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@xangarro/config": "workspace:*",
    "@xangarro/data": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.13",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
