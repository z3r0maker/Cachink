/**
 * Drizzle schema definitions land here during Phase 1B-M3 (see ROADMAP.md).
 *
 * One file per entity (sales.ts, expenses.ts, products.ts, ...) each exporting
 * a Drizzle table definition. An index.ts in this directory re-exports all
 * schemas for migration generation.
 */
export const __schemaVersion__ = 0;
