import { SuscripcionScreen } from './screen';

/**
 * Suscripción — the plan, the consumption and the receipts (P-10).
 *
 * The pricing here is **real**: every pitch, price, CTA and feature line is
 * transcribed verbatim from the design, not paraphrased (ADR-059).
 */
export default function SuscripcionPage() {
  return <SuscripcionScreen />;
}
