# Settings → Sistema → assert settings-check-for-updates visible.
# Covers Gap N (Phase 17): settings-check-for-updates testID.
#
# testIDs exercised:
#   settings-check-for-updates
#
# Precondition: wizard complete.
# x-entrypoint: wizard
appId: mx.cachink.mobile
---
- launchApp

# Dismiss any first-launch modals (ConsentModal, etc.)
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate.yaml

# Open Settings via Otros → Configuración.
- runFlow: shared/navigate-to-settings.yaml

# Navigate to Sistema sub-screen.
- tapOn:
    id: "settings-hub-sistema"
- extendedWaitUntil:
    visible: "Sistema"
    timeout: 5000

# Scroll down to find the "Check for updates" button.
- scrollUntilVisible:
    element:
      id: "settings-check-for-updates"
    direction: DOWN
    timeout: 5000

- assertVisible:
    id: "settings-check-for-updates"
