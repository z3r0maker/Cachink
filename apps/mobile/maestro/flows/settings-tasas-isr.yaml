# Settings → Tasas ISR — navigate + verify ISR defaults render.
#
# Verifies:
#   1. Navigate to Settings → Tasas ISR
#   2. ISR defaults card is visible
#   3. Save button exists
#   4. Navigate back to settings hub
#
# Precondition: wizard complete, Director role.
# x-entrypoint: wizard
appId: mx.cachink.mobile
---
- launchApp

# Dismiss any first-launch modals.
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate.yaml

# Open Settings via Otros → Configuración.
- runFlow: shared/navigate-to-settings.yaml

# Navigate to Tasas ISR sub-screen.
- tapOn:
    id: "settings-hub-tasas-isr"
- extendedWaitUntil:
    visible: "Tasas ISR"
    timeout: 5000

# Verify the ISR defaults card is visible.
- assertVisible:
    id: "isr-defaults-card"
    optional: true

# Save (tap save button to verify it exists and is interactive).
- tapOn:
    id: "isr-defaults-save"
    optional: true

# Navigate back to verify the settings hub is intact.
- tapOn:
    id: "top-bar-back"
- extendedWaitUntil:
    visible: "Ajustes"
    timeout: 5000
