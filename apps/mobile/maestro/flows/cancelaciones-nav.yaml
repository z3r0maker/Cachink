# Navigate to Cancelaciones screen and verify empty state renders.
# Route: /cancelaciones (via Otros → cancelaciones card)
# Verifies: cancelaciones-screen testID, empty-state text.
# x-entrypoint: wizard
appId: mx.cachink.mobile
tags:
  - otros
  - cancelaciones
  - regression
---
- launchApp
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate.yaml

# Review item #7: the Director bar is Inicio | Ventas | Gastos |
# Estados — no Otros tab. The tool grid moved into Configuración.
# This subflow opens it for whichever role is logged in.
- runFlow: shared/open-director-tools.yaml

- scrollUntilVisible:
    element:
      id: "otros-cancelaciones"
    direction: DOWN
    timeout: 5000
    centerElement: true
- tapOn:
    id: "otros-cancelaciones"
- extendedWaitUntil:
    visible:
      id: "mobile-cancelaciones"
    timeout: 5000

# Verify the screen title renders
- assertVisible: "Cancelaciones"
# Empty state (no sales today expected at test start)
- assertVisible: "No hay ventas registradas hoy"
