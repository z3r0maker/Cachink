# Create a new producto via the add-product affordance and verify it appears.
#
# The FAB (stock-fab) is iPhone-only; the header button (stock-nuevo-producto) renders
# on both devices and drives the same create flow. Tapping it navigates to the full
# /nuevo-producto screen (Phase 18 — no longer a modal). Required fields: nombre,
# costo, precio-venta (validateProducto).
# x-entrypoint: demo
appId: mx.cachink.mobile
---
- launchApp

# Dismiss any first-launch modals (ConsentModal, etc.)
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate.yaml

# Productos tab -> add product (header button; FAB is iPhone-only).
- tapOn:
    id: "tab-productos"
- tapOn:
    id: "stock-nuevo-producto"

# Full-page create form.
- assertVisible:
    id: "producto-nombre"
- tapOn:
    id: "producto-nombre"
- inputText: "Refresco Cola"
- tapOn:
    id: "producto-precio-venta"
- eraseText
- inputText: "25"
- tapOn:
    id: "producto-costo"
- eraseText
- inputText: "10"
- hideKeyboard

# Submit (fullWidth Btn inside the scroll — scroll to it if needed).
- scrollUntilVisible:
    element:
      id: "producto-submit"
    direction: DOWN
    timeout: 8000
- tapOn:
    id: "producto-submit"

# Back on the stock list — verify the new product via search.
- extendedWaitUntil:
    visible:
      id: "stock-buscar"
    timeout: 8000
- tapOn:
    id: "stock-buscar"
- eraseText
- inputText: "Refresco Cola"
- assertVisible:
    text: ".*Refresco Cola.*"
- hideKeyboard
