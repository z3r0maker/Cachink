# Director Home → assert cxc-card-strip renders inside cxc-card.
# Covers Gap J (Phase 17): cxc-card-strip testID.
#
# testIDs exercised:
#   cxc-card-strip
#
# Precondition: wizard complete, at least one crédito venta exists.
# Chain flows:
#
#   ./apps/mobile/maestro/scripts/fresh-install.sh \
#       apps/mobile/maestro/flows/wizard-local-standalone.yaml
#   maestro test apps/mobile/maestro/flows/inventario-producto.yaml
#   maestro test apps/mobile/maestro/flows/cliente-crear.yaml
#   maestro test apps/mobile/maestro/flows/venta-credito.yaml
#   maestro test apps/mobile/maestro/flows/director-home-cxc-strip.yaml
# x-entrypoint: wizard
appId: mx.cachink.mobile
---
- launchApp

# Dismiss any first-launch modals.
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate-director.yaml

# Director Home should be showing.
- assertVisible:
    id: "director-home-route"

# Verify the CxC card is visible (precondition: crédito venta exists).
- assertVisible:
    id: "cxc-card"

# Verify the inner strip (rows of pending credits) renders.
- assertVisible:
    id: "cxc-card-strip"
