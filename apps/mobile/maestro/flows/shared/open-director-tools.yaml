# Open the tool grid (Movimientos Caja, Cancelaciones…). With one role on the
# device (ADR-053) the grid lives in the Caja tab, below the turno.
# Precondition: signed in, any tab.
appId: mx.xangarro.mobile
---
- tapOn:
    id: 'tab-caja'
