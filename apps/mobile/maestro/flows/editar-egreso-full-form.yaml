# Edit an existing egreso changing ALL editable fields (categoría, monto, proveedor).
#
# Extends editar-egreso.yaml which only edits concepto.
# This flow exercises the remaining uncovered fields:
#   - editar-egreso-categoria
#   - editar-egreso-monto
#   - editar-egreso-proveedor
#
# Precondition: wizard complete + at least one egreso exists.
# Chain flows:
#
#   ./apps/mobile/maestro/scripts/fresh-install.sh \
#       apps/mobile/maestro/flows/wizard-local-standalone.yaml
#   maestro test apps/mobile/maestro/flows/egreso-gasto.yaml
#   maestro test apps/mobile/maestro/flows/editar-egreso-full-form.yaml
#
# The egreso-gasto flow creates "Renta del local" at $8,000.
appId: mx.cachink.mobile
---
- launchApp

# Dismiss any first-launch modals (ConsentModal, etc.)
- runFlow: shared/dismiss-modals.yaml
- runFlow: shared/authenticate.yaml

# Navigate to the Egresos tab.
- tapOn:
    id: "tab-gastos"

# Verify the existing egreso is visible.
- assertVisible: "Renta del local"

# Swipe the egreso row left to reveal the edit action.
- swipe:
    id: "egresos-list"
    direction: LEFT
    startX: 300
    endX: 50

# Wait for the EditarEgresoModal to appear.
- extendedWaitUntil:
    visible:
      id: "editar-egreso-modal"
    timeout: 5000

# --- Change categoría ---
- tapOn:
    id: "editar-egreso-categoria"
- tapOn: "Servicios"

# --- Change monto ---
- tapOn:
    id: "editar-egreso-monto"
- eraseText
- inputText: "9500"

# --- Change proveedor ---
- tapOn:
    id: "editar-egreso-proveedor"
- eraseText
- inputText: "Inmobiliaria Centro"

# Submit the edit.
- tapOn:
    id: "editar-egreso-submit"

# Modal closes and the updated egreso appears in the list.
- assertNotVisible:
    id: "editar-egreso-modal"
